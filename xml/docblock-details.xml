<?xml version="1.0" encoding="UTF-8" ?>
<p xmlns="http://pear.php.net/dtd/rest.package"    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:xlink="http://www.w3.org/1999/xlink"    xsi:schemaLocation="http://pear.php.net/dtd/rest.package    http://pear.php.net/dtd/rest.package.xsd">
 <n>docblock</n>
 <c>pecl.php.net</c>
 <ca xlink:href="/rest/c/PHP">PHP</ca>
 <l>PHP License</l>
 <s>phpDocumentor-style DocBlock (T_DOC_COMMENT token) tokenizer</s>
 <d>This extension is like the tokenizer extension for PHP.
It takes a document comment (docblock) like this one:

/**
 * information {@inlinetag}
 * @tags
 */

and parses it into tokens.

The primary function is docblock_tokenize(), which accepts a
string, and returns an array of arrays of array(TOKEN, &quot;token&quot;).
TOKEN is one of DOCBLOCK_* constants.

docblock_tokenize() has an optional second bool parameter that
determine whether to output non-essential tokens like
the /** * stuff.

docblock_token_name() takes a DOCBLOCK_* constant and returns its name</d>
 <r xlink:href="/rest/r/docblock"/>
</p>